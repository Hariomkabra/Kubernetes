apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler  # This is the HPA for the nginx deployment
metadata:
  name: nginx
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: nginx-hpa
  minReplicas: 1
  maxReplicas: 10
  metrics:
  - type: Resource
    resource:
      name: cpu
      target:
        type: Utilization
        averageUtilization: 1     # This is the target average CPU utilization